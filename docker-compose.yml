name: mm4cc-pkg
services:
  ppa-host:
    image: nginx:latest
    restart: unless-stopped
    volumes:
      - ./site-root:/usr/share/nginx/html/
  trial:
    build: ./trial
    depends_on:
      - ppa-host
